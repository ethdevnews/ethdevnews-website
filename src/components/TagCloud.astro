---
import { getCollection } from "astro:content";

const calendar = await getCollection("calendar");
const allTags = calendar.map((tag) => tag.data.tags).flat();

console.log(allTags);
---

<span> </span>
